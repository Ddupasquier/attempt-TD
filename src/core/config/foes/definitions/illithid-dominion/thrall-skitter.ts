import type { FoeType } from "../../../../../types/core/types";

const thrallSkitterFoe = {
  name: "Thrall Skitter",
  foeType: "swarm" as FoeType,
  mechanics: "fast, low HP, no armor, psychic resist 0.9",
  visual: "small, twitchy, pale glow",
};

export { thrallSkitterFoe };
