import type { FoeType } from "../../../../../types/core/types";

const drakeletFoe = {
  name: "Drakelet",
  foeType: "swarm" as FoeType,
  mechanics: "fast, low HP, light armor, fire resist 0.9",
  visual: "small winged, quick hops, ember flicker",
};

export { drakeletFoe };
