import type { FoeType } from "../../../../../types/core/types";

const hillSkirmisherFoe = {
  name: "Hill Skirmisher",
  foeType: "swarm" as FoeType,
  mechanics: "fast, low HP, no armor",
  visual: "small for giantkin, long stride",
};

export { hillSkirmisherFoe };
