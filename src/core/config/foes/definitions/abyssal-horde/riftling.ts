import type { FoeType } from "../../../../../types/core/types";

const riftlingFoe = {
  name: "Riftling",
  foeType: "swarm" as FoeType,
  mechanics: "very fast, low HP, no armor, poison resist 0.9",
  visual: "tiny, jagged, erratic movement",
};

export { riftlingFoe };
