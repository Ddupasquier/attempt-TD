import type { FoeType } from "../../../../../types/core/types";

const sparkAdeptFoe = {
  name: "Spark Adept",
  foeType: "swarm" as FoeType,
  mechanics: "fast, low HP, no armor, force resist 0.9",
  visual: "slim, flickering runes",
};

export { sparkAdeptFoe };
