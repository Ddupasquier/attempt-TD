import type { FoeType } from "../../../../../types/core/types";

const scaledSkitterFoe = {
  name: "Scaled Skitter",
  foeType: "swarm" as FoeType,
  mechanics: "fast, low HP, no armor, poison resist 0.85",
  visual: "small, serpentine, quick coils",
};

export { scaledSkitterFoe };
