# attempt-TD

Basic fantasy tower defense prototype.

Install:
- `npm install`

Run locally (Vite):
- `npm run dev`

Build:
- `npm run build`

Preview build:
- `npm run preview`

Lint:
- `npm run lint`
